<Project>
  <PropertyGroup>
    <VersionPrefix>2.0.0</VersionPrefix>
    <PackageReleaseNotes>
      ### New in 2.0.0
      * Updated `Listing.Id` and `SellerListing.Id` to be `long`
      * Include GogoKit version information in `User-Agent` header
    </PackageReleaseNotes>
  </PropertyGroup>
</Project>
